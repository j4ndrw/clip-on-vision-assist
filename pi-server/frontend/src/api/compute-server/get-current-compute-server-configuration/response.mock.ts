import type { GetCurrentComputeServerConfigurationResponse } from "./types";

export const mock: GetCurrentComputeServerConfigurationResponse = {
  computeServerConfig: {
    endpoint: "https://some.compute-server"
  }
}
